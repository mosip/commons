# idgenerator

Vert.x `:8080` · `/v1/idgenerator`+`/v1/ridgenerator`. Sole `IDGeneratorVertxApplication`. **No springdoc UI** (not servlet). Local `run-local.*`; smoke health then curl UIN/VID/RID. Path Δ → Helm/deploy.
