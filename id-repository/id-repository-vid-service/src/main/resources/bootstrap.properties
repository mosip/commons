spring.cloud.config.uri=http://104.211.212.28:51000/
spring.cloud.config.label=0.12.0
spring.cloud.config.name=id-repository
spring.application.name=id-repository
spring.profiles.active=dev

#management.security.enabled=false
#management.endpoint.health.show-details=when-authorized
#management.endpoints.web.exposure.include=*
management.endpoint.health.show-details=always
management.endpoints.web.exposure.include=info,health,refresh,restart


server.port=8091
server.servlet.path=/idrepository/v1

#disabling health check so that client doesnt try to load properties from sprint config server every
# 5 minutes (should not be done in production)
health.config.enabled=false

mosip.idrepo.ida.rest.uri=${mosip.base.url}/v1/vid/{uin}
mosip.idrepo.ida.rest.httpMethod=POST
mosip.idrepo.ida.rest.headers.mediaType=application/json
mosip.idrepo.ida.rest.timeout=1